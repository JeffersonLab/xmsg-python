#!/usr/bin/env bash
# author royarzun

if [ "$1" = "-h" -o "$1" = "-help" ]
then
    echo ""
    echo "Starts xMsgSyncPublisher"
    echo "---------------------------------"
    echo "synopsis: px_sync_publisher <array_size>"
    echo ""
else
    if ! $(python -c 'from xmsg.examples.SyncPublisher import main' &> /dev/null);
	then
		echo "Please check that you have xMsg and its dependencies installed in your system or virtual environment ";
	else
		python -c 'from xmsg.examples.SyncPublisher import main; main('$1')'
	fi

fi
